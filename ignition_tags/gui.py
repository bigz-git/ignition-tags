# TODO: Build Tkinter GUI that wraps the three CLI commands.
#
# The GUI should import directly from core.py — no conversion logic here:
#
#   from .core import build_tag_provider, build_udt_types, flatten_tags
#
# Each button should:
#   1. Open file dialogs to collect input/output paths and any extra args
#      (provider name, OPC server, UDT format, etc.)
#   2. Call the relevant core function
#   3. Show a messagebox with the result or error
#
# See archive/ignition_tag_tool_r9.py for the original GUI as a reference.
